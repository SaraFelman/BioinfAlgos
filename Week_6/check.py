import re

# text = "Самолет прилетает 03/25/2024. Будем рады вас видеть после 04/01/2024."
# print(re.sub(r'(\d\d)/(\d\d)/(\d{4})', r'\2.\1.\3', text))


# print(re.sub(r'(\w+)(\s)(\d.\d)', r'\3\2\1', "Творчество Gemini 1.5 Flash 002"))

regexp = re.compile(r'\d\d\d\d\d')
match = re.search(r'('
https: // \ * \)', i)  # 3)

print (match[0] if match else "Не найден")